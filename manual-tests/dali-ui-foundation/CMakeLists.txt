CMAKE_MINIMUM_REQUIRED(VERSION 3.8.2)

PROJECT(manual-test-dali-ui-foundation)

SET(CMAKE_CXX_STANDARD 17)
SET(CMAKE_C_STANDARD 99)

FIND_PACKAGE(PkgConfig REQUIRED)
PKG_CHECK_MODULES(DALICORE REQUIRED dali2-core)
PKG_CHECK_MODULES(DALIADAPTOR REQUIRED dali2-adaptor)
PKG_CHECK_MODULES(DALI_UI_FOUNDATION REQUIRED dali2-ui-foundation)
PKG_CHECK_MODULES(DALI_UI_COMPONENTS REQUIRED dali2-ui-components)

SET(EXE_NAME manual-test-dali-ui-foundation)

# Auto-discover TC source files from the tc/ subdirectory.
# Each .cpp file in tc/ is compiled into the same binary so its static
# initializer runs at startup and registers the TC with the launcher.
FILE(GLOB TC_SOURCES "${CMAKE_CURRENT_SOURCE_DIR}/tc/*.cpp")

ADD_EXECUTABLE(${EXE_NAME}
  main.cpp
  ${TC_SOURCES}
)

SET_TARGET_PROPERTIES(${EXE_NAME} PROPERTIES
  RUNTIME_OUTPUT_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/bin")

INSTALL(TARGETS ${EXE_NAME} RUNTIME DESTINATION)

TARGET_LINK_LIBRARIES(${EXE_NAME}
  ${DALICORE_LIBRARIES}
  ${DALIADAPTOR_LIBRARIES}
  ${DALI_UI_FOUNDATION_LIBRARIES}
  ${DALI_UI_COMPONENTS_LIBRARIES}
)

TARGET_LINK_DIRECTORIES(${EXE_NAME} PRIVATE
  $ENV{DESKTOP_PREFIX}/lib
)

TARGET_INCLUDE_DIRECTORIES(${EXE_NAME} PRIVATE
  ${CMAKE_CURRENT_SOURCE_DIR}
  ${DALICORE_INCLUDE_DIRS}
  ${DALIADAPTOR_INCLUDE_DIRS}
  ${DALI_UI_FOUNDATION_INCLUDE_DIRS}
  ${DALI_UI_COMPONENTS_INCLUDE_DIRS}
)

TARGET_COMPILE_OPTIONS(${EXE_NAME} PRIVATE
  ${DALICORE_CFLAGS_OTHER}
  ${DALIADAPTOR_CFLAGS_OTHER}
  ${DALI_UI_FOUNDATION_CFLAGS_OTHER}
  ${DALI_UI_COMPONENTS_CFLAGS_OTHER}
)

# Expose the local res/ directory path to all TC source files.
# Each TC file can reference assets via:  IMG_LANDSCAPE = TEST_RESOURCE_DIR "/landscape-sample.jpg"
SET(TEST_RESOURCE_DIR "${CMAKE_CURRENT_SOURCE_DIR}/res")
TARGET_COMPILE_DEFINITIONS(${EXE_NAME} PRIVATE TEST_RESOURCE_DIR="${TEST_RESOURCE_DIR}")
